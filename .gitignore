recorder/audiodevs
recorder/systemtap/.build/
recorder/miccap/.build/
recorder/diarize/.build/
# Fetched by fetch-models.sh (SAA-194), verified against the checked-in
# CHECKSUMS.sha256 — the 21 MiB of model weights themselves don't belong in
# git.
recorder/diarize/models/speaker-diarization/
recorder/app/node_modules/
recorder/app/dist/
recorder/diagnostics/audioinuse
# Built on demand by menubarwatch.sh, same as audioinuse above.
recorder/diagnostics/menubarscan
recorder/micwatch

# Local secrets and OS cruft — never commit these to a public repo
.env
.env.*
!.env.example
.DS_Store

# Local Claude Code session state
.claude/
